spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true 
eureka:
    client:
      serviceUrl:
        defaultZone: http://discovery:8761/eureka/
server:
  port: 8222
