spring:
  cloud:
    gateway:
      discovery:
         enable : true
    routes:
        - id: product-service
          uri: lb://product-service
          predicates:
            - Path=/api/v1/product1/**
eureka:
    client:
      serviceUrl:
        defaultZone: http://discovery:8761/eureka/
server:
  port: 8222
