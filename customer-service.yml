spring:
  data:
    mongodb:
      username: medamine
      password: medamine
      host: mongodb
      port: 27017
      database : customer
      authentication-database: admin
eureka:
    client:
      serviceUrl:
        defaultZone: http://discovery:8761/eureka/
      fetch-registry: true
      register-with-eureka: true
server:
  port: 8090

